<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="2dp"
    android:gravity="center_vertical"
    android:minHeight="@dimen/game_overlay_bottom_bar_height"
    android:minWidth="@dimen/game_overlay_player_item_width"
    android:orientation="horizontal">

    <View
        android:id="@+id/game_overlay_item_border_left"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:background="@color/colorPrimary"
        android:visibility="gone"
        tools:background="@color/colorOrangeWarning"
        tools:visibility="visible" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:minHeight="@dimen/game_overlay_bottom_bar_height"
        android:minWidth="@dimen/game_overlay_player_item_width"
        android:orientation="horizontal"
        android:padding="6dp"
        tools:background="@color/colorTransparentTutorial">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:src="@drawable/dots" />

    </LinearLayout>

    <View
        android:id="@+id/game_overlay_item_border_right"
        android:layout_width="1dp"
        android:layout_height="match_parent"
        android:background="@color/colorPrimary"
        android:visibility="gone"
        tools:background="@color/colorOrangeWarning"
        tools:visibility="visible" />

</LinearLayout>